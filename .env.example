# Base URL of the indeed-whatsapp-webhook NestJS backend
BACKEND_API_URL=http://localhost:3111

# Shared secret: gates the admin login page AND is sent as "x-admin-secret"
# to the backend's /jobs, /contacts, /templates, /submissions routes.
ADMIN_SECRET=change_me
